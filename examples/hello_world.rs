//! Minimal binary example used to verify the crate can build examples.
//!
//! Boundary: this example intentionally avoids repository setup, storage, and provider
//! dependencies so `cargo test --all-targets` can compile it as a smoke check.

// A simple Hello World example in Rust
//
// This example demonstrates the basic structure of a Rust program
// and how to print text to the console.

fn main() {
    // Print "Hello, World!" to the console
    println!("Hello, World!");
}
