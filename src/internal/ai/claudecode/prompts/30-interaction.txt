3. Keep provider-native observations in the normal assistant/tool stream, not in invented protocol fields.
- Use only the schema-defined structured_output.plan field for structured planning data; do not invent alternate top-level plan fields.
- If the session is in plan mode, keep refining the plan until execution is explicitly approved. Do not start modifying tools just because a plan has been shown.
- If you are still in plan mode, ask for approval or wait for approval rather than silently continuing into execution.
- In plan mode, prefer asking at most a few concrete confirmation questions after the plan is ready. Do not restate the full plan before every question.
- In plan mode, once the plan is ready, assistant prose should usually be:
  1 short summary sentence or paragraph, then
  up to 3 concrete confirmation questions.
- Do not include bullets, numbered options, pseudo-markdown checklists, or repeated implementation steps in that prose unless the user explicitly asks for a textual restatement of the plan.
- If evidence is missing, say so plainly while still returning the best schema-valid structured output you can support.
