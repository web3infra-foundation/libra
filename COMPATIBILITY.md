# Libra Compatibility Matrix

> 4 tiers: `supported` / `partial` / `unsupported` / `intentionally-different`
> Source of truth: top-level `Commands` variants in [`src/cli.rs`](src/cli.rs).

This file declares which Git command surfaces Libra promises to support, and at
what level. The four tiers have the following user-facing semantics:

| Tier | Meaning | What users should expect |
|------|---------|--------------------------|
| `supported` | Command/flag behavior matches stock Git or is functionally equivalent | Use as you would in Git |
| `partial` | Command is exposed but the subcommand surface or flag set is incomplete | Common paths work; advanced paths may be missing |
| `unsupported` | Not implemented, no public plumbing | Use stock Git or look for an equivalent Libra command |
| `intentionally-different` | Behavior deliberately diverges from Git; documented | Read the migration notes before relying on it |

The tier here describes **Git surface** compatibility only. It does not
describe whether a command has been modernized for CLIG (`--json` / `--machine`
/ stable error codes / `run_<cmd>()` split); that work is tracked in
[`docs/improvement/README.md`](docs/improvement/README.md) and in each command
batch document.

## Top-level commands (from `src/cli.rs`)

| Command | Tier | Notes |
|---------|------|-------|
| init | supported | vault-backed signing by default (`--vault false` to skip); `--shared[=<mode>]` persists `core.sharedRepository` and applies group/world permissions to the `.libra/` content tree while keeping `vault.db` (`0o600`) and the `.libra/` root entry owner-only writable (no-op on Windows); safe re-init tops up missing templates + refreshes `--shared` while preserving `libra.repoid`/`vault.db`/refs, and rejects an explicit conflicting `--object-format`/`--ref-format` (`LBR-CLI-002`); `--separate-git-dir`/`--separate-libra-dir` intentionally removed (always co-located `.libra/`) |
| clone | partial | `--depth`, `--single-branch`/`--no-single-branch`, `--shallow-since`, `--shallow-exclude` (repeatable), `--reject-shallow`, `-o/--origin`, and `--no-checkout` supported (`--depth` may be combined with `--shallow-since`/`--shallow-exclude`; the time/ref request supersedes plain depth per the upload-pack protocol); `--mirror` partial (implies bare, writes `remote.<name>.mirror = true` + `+refs/*:refs/*` refspec config, and clones all branch heads; branch refs are stored as remote-tracking and tags/other namespaces are not yet mirrored at their exact `refs/*` names — full mirror is a known limitation); `--reference`/`--reference-if-able`/`--dissociate` intentionally-different (copy semantics — source objects are copied into local tiered storage, no long-term `info/alternates` borrow; `--dissociate` confirms the clone is fully local); `-l/--local` supported (hardlinks local-source objects, `--no-hardlinks` copies, symlink sources rejected); `-s/--shared` intentionally-different (copy semantics, no alternates); `-j/--jobs` intentionally-different (validated 1..=16 and reserved/no-op — transport is serial; Git's submodule-fetch semantics do not apply); `--filter` partial (whitelist `blob:none`/`blob:limit=<n>`/`tree:<depth>`; records promisor config; no lazy backfill — pair with `--no-checkout`/`--bare`; requires server `uploadpack.allowFilter`); `--sparse` unsupported (see [docs/improvement/compatibility/declined.md#d10-clone---sparse-与顶层-sparse-checkout-命令](docs/improvement/compatibility/declined.md#d10-clone---sparse-与顶层-sparse-checkout-命令)); `--recurse-submodules` unsupported (see [docs/improvement/compatibility/declined.md#d4-clone---recurse-submodules](docs/improvement/compatibility/declined.md#d4-clone---recurse-submodules)) |
| code | intentionally-different | Libra AI extension, not a Git command |
| code-control | intentionally-different | Libra AI automation extension, not a Git command |
| automation | intentionally-different | Libra AI automation rules/history extension, not a Git command |
| usage | intentionally-different | Libra AI provider/model usage reporting extension, not a Git command |
| graph | intentionally-different | Libra AI graph inspection extension, not a Git command |
| sandbox | intentionally-different | Libra AI sandbox diagnostics extension, not a Git command |
| package | intentionally-different | Libra AI capability-package install/list/diff extension, not a Git command |
| stats | intentionally-different | Libra working-tree file-statistics extension (read-only: counts files grouped by extension, ignoring `.libra/` and `target/`; `--json`/`--machine` output), not a Git command |
| add | partial | `--chmod=(+\|-)x` supported (index mode only, not worktree perms); `--renormalize` intentionally-different (force-rewrites tracked entries' blobs; no CRLF/EOL normalization — git-internal has no clean filter); `--pathspec-from-file` / `--pathspec-file-nul` partial (no quoted/escaped pathspec — raw bytes; 128 MiB cap); `--ignore-missing` partial/intentionally-different (dry-run only; missing paths skipped with a warning rather than the Git ignored-even-if-missing check); `add.ignoreErrors` config honored; `--sparse` unsupported (declined); `-N` / `--intent-to-add` deferred (on-disk index has no intent-to-add bit); `-p`/`--patch` rejected (interactive patch UI incompatible with Agent-driven automation, see [docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族](docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族)) |
| rm | partial | `--force` / `--dry-run` / `--cached` / `--recursive` / `--ignore-unmatch` / `--pathspec-from-file` / `--pathspec-file-nul` supported; human output respects `--color` / `--no-color` and uses plain `rm '<path>'` when piped; uncommitted-change refusals use warning exit 9 with four-space path indentation; real removals save `.libra/index` before deleting working-tree files, while filesystem removal failures aggregate into `LBR-WARN-001` with `details.failed_paths`. Sparse-checkout flag unsupported; per-command `--quiet` not exposed (use global `--quiet`) |
| mv | partial | `--sparse` accepted as no-op (sparse-checkout cone not implemented); submodule cascade rename out of scope |
| restore | partial | `--source`/`-s`, `--staged`/`-S`, `--worktree`/`-W`, pathspec restore, default `--no-overlay`, `--overlay`, and `--pathspec-from-file`/`--pathspec-file-nul` supported. Conflict-stage restore `--ours`/`-2`, `--theirs`/`-3`, `--merge`, and `--conflict=merge\|diff3` write only the working tree and leave the index unmerged; a plain restore over an unmerged path is refused (`LBR-CONFLICT-001`, exit 128) and `--ignore-unmerged` skips unmerged paths. Deferred / intentional differences: `--conflict=zdiff3`, `-p`/`--patch` (see [docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族](docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族)), Git C-quoting for non-NUL pathspec files, restore-time `core.autocrlf` renormalization, and a 50 MiB safety cap for conflict-marker rendering |
| status | partial | Short, long, porcelain v1/v2, `--branch`, `--show-stash`, `--ignored`, `--untracked-files=<normal\|all\|no>`, `-z`, and `--exit-code` supported; `status.showUntrackedFiles`, `status.branch`, and `status.short` config defaults are honored with CLI override. Porcelain v2 emits branch headers, ordinary `1` rows, staged rename `2` rows with fixed 50% similarity scoring, unmerged `u` rows, and typechange `T` status; `-z` uses NUL record terminators and NUL-separated rename paths. JSON includes `upstream`, `merge_state`, `repo_state` (merge/rebase/bisect), `stash_entries` (when requested), and `renames`; long human output reports rebase/bisect in-progress hints. Deferred: porcelain v1 rename rows, copy detection, pathspec filtering, submodule status details, cherry-pick in-progress state, and Git rename controls/config (`--find-renames`/`-M`, `--renames`, `--no-renames`, `status.renames`, `status.renameLimit`) |
| clean | partial | `-n`/`-f`/`-d`/`-x`/`-X`/`--exclude` supported; `-e` short alias for `--exclude`; `force` is a repeatable count (`-ff` required to also remove nested `.git`/`.libra` repositories, which are otherwise skipped with a `Skipping repository <path>` warning); `clean.requireForce` config honored (default `true`, blocks a mode-less invocation; set `false` to allow `clean` with no flag); `-i`/`--interactive` selection loop (`clean`/filter-by-pattern/select-by-numbers/ask-each/quit/help) — intentionally-different: mutually exclusive with `--json` and `-n` (`LBR-CLI-002`); removal is tolerant (a per-path failure is warned and recorded in the JSON `failed` field, cleanup continues, exit 128). Remaining gap: `<pathspec>...` positional filter is **not supported** (declined — would be a separate plan) |
| stash | partial | `push` / `pop` / `list` / `apply` / `drop` / `show` / `branch` / `clear` supported. `stash push` supports `-m`, `-u` / `--include-untracked`, `-a` / `--all`, and `--keep-index`; included untracked/ignored files are stored in a third stash parent and restored by `apply` / `pop`. `stash show` supports `--stat`, `--name-only`, `--name-status`, and reads `stash.showPatch` / `stash.showStat` defaults. Deferred: `push -- <pathspec>`, `pop/apply --index`, conflict-marker/unmerged-index application, `create`, `store` (see [docs/improvement/compatibility/declined.md#d8-stash-create](docs/improvement/compatibility/declined.md#d8-stash-create) and [#d9-stash-store](docs/improvement/compatibility/declined.md#d9-stash-store)), and `-p`/`--patch` in show (see [docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族](docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族)) |
| lfs | partial | built-in Libra LFS command; uses `.libra_attributes`, **not** Git LFS smudge/clean filters or hooks (intentional difference D5, see [docs/improvement/compatibility/declined.md#d5-git-lfs-gitattributes-filter--hooks-bridge](docs/improvement/compatibility/declined.md#d5-git-lfs-gitattributes-filter--hooks-bridge)). Subcommands: `track`/`untrack`/`ls-files`/`locks`/`lock`/`unlock` supported; `install`/`uninstall` are exit-0 no-op compat shims (no global filter/hook install); `push`/`fetch` partial (commit-graph pointer scanner + LFS batch protocol — push is current-branch-only, no transfer-adapter negotiation); `prune` partial (keep set = branches/tags/HEAD/reflog/index/worktree-pointer reachable OIDs; no remote-tracking or `--recent` window); `checkout` restores worktree pointer files from the local cache (hash-verified) |
| log | partial | core history walk + `--oneline`/`--graph`(rotating `--color`)/`--pretty` (custom placeholders `%H %h %T %t %P %p %s %f %b %B %an/%ae/%ad %cn/%ce/%cd %d %n %x<HH>`, `format:`/`tformat:`)/`--stat`/`--patch`/`--name-only`/`--name-status`; filters `--author`/`--committer`/`--since`/`--until`/`--grep`(regex, `-i`)/`--merges`/`--no-merges`/`--min-parents`/`--max-parents`/`--first-parent`; pickaxe `-S`/`-G`; revision ranges `A..B`/`A...B`/`^A B`, including `A..B -- path` pathspec filtering with worktree-escape rejection; `--follow` single-file rename tracking (multi-path follow rejected with `LBR-CLI-002`; human `--name-status` renders `R<score>` while JSON keeps added/deleted statuses). Deferred/missing: `--all`/`--branches`/`--tags` multi-root, `-L` line-log, full `--mailmap`, `--reverse`, octopus-merge graph connectors |
| shortlog | partial | `-n`/`-s`/`-e`, `-c`/`--committer`, `--no-merges`, `--since`/`--until`, single revision, `A..B` double-dot ranges, root `.mailmap`, `-w` human wrapping, limited `--format`, and JSON/`--machine` supported. Intentionally different: custom width values use `-w=<spec>` rather than Git's `-w<spec>` spelling, and human subjects over 64 KiB are truncated with a warning while JSON stays full. Unsupported: stdin log parsing, pathspecs, `--all`/`--branches`/`--tags`, three-dot/`^ref` revision syntax, `--group`, `mailmap.file`, and `mailmap.blob` |
| show | partial | Commit/tag/tree/blob display, multiple objects in command-line order, `REV:path`, `--no-patch`/`-s`, `--patch`/`-p`, `--oneline`, `--pretty` presets (`oneline`/`short`/`medium`/`full`/`fuller`), limited `--format` placeholders (`%H`/`%h`/`%s`/`%n`/`%an`/`%ae`/`%cn`/`%ce`/`%%`), `--name-only`, `--name-status`, `--stat`, pathspec filtering, and typed JSON output supported. Single-object JSON keeps `data` as one object; multi-object JSON returns `data` as an array. Intentionally different: binary blobs and blobs larger than 10 MiB are summarized with metadata instead of printing raw content; use `cat-file -p` for raw blob bytes. Deferred: full Git `--pretty`/`--format` mini-language, `-U`/`--unified`, and `diff.noprefix` integration |
| show-ref | partial | `--heads`/`--tags`/`--head`/`-s`/`--hash`, Git-style path-segment-suffix pattern filtering, `-d`/`--dereference` (peeled `^{}` lines for annotated tags), `--exists <ref>` (existence probe: exit 0/2, no stdout — checks the ref record only), and `--verify` (exact full-refname mode; miss=128, `--quiet` miss=1; combines with `--dereference` for annotated tags) supported. Deferred: `--exclude-existing` and `--abbrev`/`--hash=<n>` width |
| ls-remote | partial | `--heads`/`--tags`/`--refs`/patterns + `--exit-code` (silent exit 2 on no match) supported; `--symref` supported for HTTP/SSH/git remotes but intentionally-different for local paths (Libra's local discovery advertises no capabilities, so no `ref:` lines); `--get-url` partial (offline + credential-redacted, no `url.*.insteadOf` rewriting); `--sort` partial (`refname`/`version:refname` subset; other for-each-ref keys → `LBR-CLI-002`); `-o`/`--server-option` partial (parsed, not forwarded); `--upload-pack`/`-b`/per-command `-q`/default `<repository>` unsupported |
| ls-files | partial | Basic index/worktree enumeration is implemented: cached entries, `--cached`, `--deleted`, `--modified`, `--stage`, `-s`, `--others`, and `--exclude-standard` are supported with `--json`/`--machine`. Deferred: pathspecs, `-z`, `--error-unmatch`, `--ignored` modes, explicit exclude flags, `--eol`, resolve-undo, killed/diagnostic/debug output, and sparse-checkout integration. |
| ls-tree | partial | Lists commit/tree contents with Git-style default output, `-r`/`--recursive`, `-t`, `-d`, `-l`/`--long`, `-z`, `--name-only`, `--name-status`, `--object-only`, `--abbrev[=<n>]`, repository-relative path prefix filters, and `--json`/`--machine` (Libra extension). Deferred: full Git pathspec/glob semantics, `REV:path` tree-ish syntax, `--format`, `--full-name`, and `--full-tree`. |
| symbolic-ref | partial | Supports local `HEAD` only (read/`--short`/set/`-q`); other symbolic ref names are rejected because Libra stores refs in SQLite. `-m <reason>` records the exact HEAD reflog message when the target branch has a commit; unborn targets update HEAD without writing an invalid reflog row. `-d`/`--delete` is intentionally rejected (`LBR-CONFLICT-002`; HEAD is the required root symbolic ref). `--recurse`/`--no-recurse` are not exposed because Libra has no symbolic-ref chains |
| branch | partial | SQLite-backed refs (reference table, not `.git/refs`). List filters `--contains`/`--no-contains`/`--merged`/`--no-merged`/`--points-at` and `--ignore-case` sort supported; upstream set (`-u`) / unset (`--unset-upstream`) with tracking shown in list; copy (`-c`/`-C`) and transaction-hardened rename with config + conditional reflog migration; `--edit-description` supported; `-f`/`--force` resets existing create/copy targets (locked branches always refused); `--create-reflog` accepted-but-no-op (no per-branch reflog). `--track`/`--no-track` declined (intentionally-different — use `libra switch --track` or `-u`); `--sort`/`--format` unsupported (accepted-but-ignored, use `--json`). Locked branches (main/intent/agent-traces) are protected from destructive ops (intentionally-different) |
| tag | partial | create (lightweight, or annotated via `-m`/`-a`/`-F`/`-e`), `-f`/`--force`, multi-tag `-d`/`--delete`, `-l`/`--list` + `-n`/`--n-lines`, `--points-at <object>`, `--contains`/`--no-contains`, `--merged`/`--no-merged`, and `--sort=refname\|-refname\|creatordate\|-creatordate` supported. `-a` without a message opens the configured editor; without an editor it fails with `LBR-CLI-002` instead of silently creating a lightweight tag. Batch delete returns additive JSON `deleted`/`failed` arrays and exits 128 if any item fails. Deferred: `-s`/`-u`/`-v` Vault tag signing/verification and broader Git sort keys. |
| notes | partial | `add` / `list` / `show` / `remove` supported; `append` / `edit` / `copy` / `merge` / `prune` / `get-ref` not implemented. Notes refs are local-only — push/fetch/clone does not transfer them yet |
| commit | partial | Core staged-commit path plus message control (`-m`/`-F`, `-e`/`--edit`, `--no-edit` outside `--amend`, `-t`/`--template` + `commit.template`, `--cleanup=strip/whitespace/verbatim/scissors/default`), editor cascade (`$GIT_EDITOR`→`core.editor`→`$VISUAL`→`$EDITOR`→`vi`; non-TTY without an editor errors instead of hanging), `-a`/`--all`, `-s`/`--signoff`, `--author`, `--allow-empty`, `--conventional`, `--fixup <commit>`/`--squash <commit>` autosquash, `--dry-run`/`--porcelain`, `-v`/`--verbose`, and `--json`/`--machine` supported. Intentionally-different: `-S`/`--gpg-sign`/`--no-gpg-sign` + `commit.gpgSign` drive **vault-backed PGP** signing (not an external GnuPG process); coarse exit codes (nothing-to-commit/hook/sign failure → 128, not Git's 1); `--porcelain` must be combined with `--dry-run` (Git's implies dry-run); `prepare-commit-msg`/`commit-msg` run from Libra-native `.libra/hooks/*.sh|.ps1` (see [Hooks](#hooks)). Deferred/unsupported: `post-commit` hook, external GnuPG, `--allow-empty-message`, the `--fixup=amend:/reword:` extended forms, and `-p`/`--patch` (see [docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族](docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族)) |
| switch | partial | `-c`/`--create`, `-C`/`--force-create` (create or reset existing), `-f`/`--force`/`--discard-changes`, `--orphan`, `--guess`/`--no-guess` with `checkout.guess` and `checkout.defaultRemote`, `-d`/`--detach`, and `--track` (boolean upstream set) supported, with locked/AI-managed-branch protection. `--orphan` leaves `SwitchOutput.commit=""` for the unborn branch and clears tracked files from index/worktree. Deferred: `-m`/`--merge`/`--conflict`, `--track=direct\|inherit`/`--no-track` (Libra `--track` is always direct), `--ignore-other-worktrees`, `--overwrite-ignore`; `--recurse-submodules`/`--progress` intentionally-different. Only `--no-guess` negated form is offered; other `--no-*` switch negations are not generated. `-q` via global `--quiet` |
| rebase | partial | Linear rebase with persistent SQLite state, `--continue`/`--abort`/`--skip`, structured JSON/machine output, typed criss-cross merge-base rejection, `--onto <newbase> <upstream> [<branch>]`, `--autostash`/`rebase.autoStash` with OID-based stash restore, non-interactive `--autosquash`/`rebase.autoSquash`, redundant cherry-pick filtering with `--reapply-cherry-picks`, partial `--root` (single-root histories), empty-commit controls (`--keep-empty`, `--empty=drop\|keep\|stop`), `--signoff`, and vault-backed `-S`/`--gpg-sign` supported. Intentionally different: no external GnuPG/keyid selection for `-S`; autosquash does not open an interactive todo editor. Deferred/unsupported: interactive rebase (`-i`/`--interactive`, `--edit-todo`, see [docs/improvement/compatibility/declined.md#d16-交互式-rebase-i--interactive-与-edit-todo](docs/improvement/compatibility/declined.md#d16-交互式-rebase-i--interactive-与-edit-todo)), `--exec`, `--rebase-merges`, `--force-rebase`, and `--empty=ask` |
| merge | supported | fast-forward, best-base single-head three-way with recursive virtual merge bases for criss-cross histories, clean disjoint octopus, `--squash`, `--no-ff`/`--ff-only`/`merge.ff`, `--no-commit`/`merge.commit`, `-m`/`-F`/`--signoff`/`--log`/`--into-name`, `-e`/`--edit`, `ours` strategy, `-X ours/theirs`, diff3 markers, `--stat`/`merge.stat`, `--autostash`/`merge.autoStash`, `-S`/`--gpg-sign` vault signing, `--verify-signatures`/`merge.verifySignatures`, whitespace-insensitive merge, rename detection (`--find-renames[=<n>]`/`merge.renames`/`merge.renameLimit`), and `--diff-algorithm`/`--cleanup` validation. Remaining gaps: subtree/custom strategies and merge drivers, conflicted-octopus resolution, and directory-rename tracking are deferred; signature verification is a presence check, not cryptographic (see [docs/improvement/compatibility/merge.md](docs/improvement/compatibility/merge.md)) |
| reset | partial | `--soft`/`--mixed`/`--hard` plus `--merge`/`--keep` safety resets and pathspec un-staging supported, with index rollback on failure. `--merge`/`--keep` fail closed with `LBR-CONFLICT-002` and JSON `details.conflict_files` before writing when local changes would be overwritten. `--pathspec-from-file`/`--pathspec-file-nul` supported for bulk/stdin pathspec input, but paths are taken literally — Git's default-mode C-style quoted-path decoding is intentionally not performed (use `--pathspec-file-nul` for special characters). `--no-refresh` is accepted as a no-op (Libra's reset never refreshes the index, so there is no refresh to skip; no `--refresh`). `-p`/`--patch` rejected (see [docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族](docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族)). |
| rev-parse | supported | Supports `--verify`, `--short`, `--abbrev-ref`, path/state flags, `--sq`/`--sq-quote`, `A..B`/`A...B`/`^A`, and JSON envelopes. Intentional differences: `--git-dir` returns `.libra`, invalid targets without `--verify` use Libra's 129 invalid-target exit, `--verify` is a single-object mode, and leading `--` before `--sq-quote` literals is consumed by clap. `--symbolic*` covers branches/remotes/tags and remains partial for obscure Git forms. |
| rev-list | supported | |
| describe | partial | `--tags`, `--abbrev` (fixed default 7 — intentionally-different), `--always`, `--first-parent`, `--exact-match`, `--match`/`--exclude` (wax globs, ≤256 chars), `--dirty[=<mark>]` (tracked changes only), `--contains` (`<refname>~N`), `--candidates`/`describe.maxCandidates`, and `--json`/`--machine` (Libra extension) supported. `--all` is partial — it extends `--contains` to local branch heads and remote-tracking branches but not `refs/notes`/`refs/stash`, and does not change the default forward describe. `--candidates` bounds candidate collection but the bounded BFS still returns the topologically nearest tag. `--long`, `--broken`, and describe-a-blob are declined |
| cherry-pick | partial | `-x` (off by default), `-s`/`--signoff`, `-e`/`--edit`, `--allow-empty`/`--allow-empty-message`/`--keep-redundant-commits`, `-m <parent-number>` (merge commits apply along the named parent only), `--ff`, and `-S`/`--gpg-sign` (reuses the vault chain, same as `merge`) supported; multi-commit `--no-commit` accumulates into the index. Conflict sequencer `--continue`/`--skip`/`--abort`/`--quit` persists in the SQLite `cherry_pick_state` table — not a `.git`/`.libra` sequencer file (intentionally-different); conflict detection is path-level, not Git's line-level hunk merge. `--strategy`/`-X`, `--empty`, `--cleanup`, `--rerere-autoupdate`, and `--commit` are unsupported (`LBR-UNSUPPORTED-001`). A `--no-commit` multi-commit conflict is terminal (no continuation) |
| push | partial | branch/tag update, multi-refspec, delete, `--tags`, and `--mirror` supported; `--force-with-lease[=<ref>[:<expect>]]` (validates the remote still matches the tracking-ref/expected OID before sending; conflicts with `--force`), `--atomic` (requires remote `atomic` capability and updates local remote-tracking refs in one SQLite transaction), `--follow-tags`/`push.followTags` (adds missing annotated tags reachable from pushed branch tips), `--push-option`/`-o` (requires remote `push-options`, validates ≤1 KiB and rejects NUL/newline), `--signed[=true\|false\|if-asked]`/`push.gpgSign` (vault-backed push certificate when the remote advertises `push-cert`; `if-asked` skips when unsupported), and `--porcelain` (machine-readable per-ref lines; conflicts with `--json`/`--machine`) supported; `--force-if-includes` and `--thin`/`--no-thin` accepted as **no-ops** (lease uses tracking-ref OID only; the pack encoder is always self-contained). local file remote rejected — intentional (see [docs/improvement/compatibility/declined.md#d2-本地-file-remote-的-push](docs/improvement/compatibility/declined.md#d2-本地-file-remote-的-push)) |
| fetch | partial | `--all`, refspec, `--depth`/`--deepen`/`--unshallow`, `--shallow-since`/`--shallow-exclude`, `--prune`/`fetch.prune`, `--porcelain`, `--dry-run`, FETCH_HEAD write + `--append` (long-only; `-a` stays `--all`, intentionally-different from Git's `-a`=`--append`), `-v`/`--verbose` (announces the remote being contacted on stderr; stdout contract unchanged), default tag auto-follow (imports lightweight/annotated tags that point at objects fetched in the same operation), `-t`/`--tags`/`--no-tags` + `remote.<name>.tagOpt` (`--tags` imports every advertised tag into global `refs/tags/*`, pulling each tag's object; `--no-tags` disables auto-follow; existing local tags are preserved without `--force`; `--no-tags` is long-only, intentionally-different from Git's `-n`), `-f`/`--force` (clobber an existing local tag; rendered as porcelain `+`; remote-tracking refs always update regardless), `--update-shallow` (accept new shallow boundaries from a shallow remote even without a shallow operation; boundary removals always apply), `--atomic` (per-remote ref updates already commit together in one SQLite transaction; on rollback the downloaded pack is removed so nothing partial is left), `--refmap <src:dst>` (override fetched-ref destinations; repeatable, ≤256 bytes/entry, destination must be under `refs/remotes/<remote>/`, requires an explicit remote — not `--all`), and `--json`/`--machine` (Libra extension) supported. `--deepen` uses Git's relative `deepen-relative` request and conflicts with `--depth`/`--unshallow`. `--recurse-submodules` is declined (intentionally-different, exit 129). |
| pull | partial | fetch + fast-forward/three-way merge supported; `--ff-only` and `--rebase` (`-r`) strategy flags exposed; merge-engine flags `--squash`/`--no-squash`, `--commit`/`--no-commit`, `--ff`/`--no-ff`, `--autostash`/`--no-autostash` forwarded (merge path only) plus fetch `--depth`; reads `pull.rebase`/`pull.ff` (CLI overrides) and falls back to `merge.commit`/`merge.autoStash`/`merge.ff`. **unsupported:** pull does not expose fetch's `--unshallow`/`--deepen`/advanced shallow flags, `--rebase=merges`/`--rebase=interactive` (engine does linear rebase only — rejected, not silently degraded), and pull still rejects `--autostash` on the rebase path instead of forwarding it to `libra rebase --autostash` |
| for-each-ref | partial | Enumerates local branch refs, remote-tracking refs, and tag refs from Libra's SQLite-backed ref model. Supports `--heads`, `--tags`, `--remotes`, `--all`, `--format` atoms `%(refname)`, `%(objectname)`, `%(objecttype)`, `--sort=refname|-refname|objectname|-objectname`, `--count`, patterns, and `--json`/`--machine`. Deferred: full Git atom language, full sort/filter keys, `--contains`/`--no-contains`, `--merged`/`--no-merged`, `--points-at`, shell/perl/python/tcl quoting modes, and Git packed-refs layout parity (not applicable to Libra storage). |
| diff | partial | `--old`/`--new`/`--staged`, pathspec, `--stat`/`--numstat`/`--name-only`/`--name-status`/`--raw` (including mode-only `T` typechange entries), `-b`/`-w`/`--ignore-blank-lines`, `-U<n>`/`diff.context`, `--exit-code`, `-M`/`-C`/`--no-renames`/`diff.renames`/`diff.renameLimit`, `--relative`, `--word-diff[=plain]`/`--word-diff-regex`/`diff.wordRegex`, `-W`/`--function-context`, `diff.noPrefix`, and `--json`/`--machine` (Libra extension) supported. `--word-diff=color` is intentionally-different (Libra colour codes). `--cc`/`--combined`, `.gitattributes` diff drivers, and `diff.mnemonicprefix` are deferred. `--algorithm=myers`/`myersMinimal` is unsupported (fail-closed `LBR-CLI-002`; only `histogram` validates, backed by git-internal's Myers). |
| grep | partial | `-A`/`-B`/`-C` context lines, `--heading`/`--no-heading`, `--break`, `-z`/`--null` (NUL field separators), `-a`/`--text` + `-I` binary handling, `--no-index` (search the filesystem without a repo; prunes `.libra`/`.git`, no symlink following), and `--untracked` (also search untracked, non-`.libraignore`'d files) are supported; `--cached`/`--tree`/`--no-index`/`--untracked` are mutually exclusive (usage error 129). `-E`/`-G` are accepted as aliases (the default Rust `regex` engine is ERE-style; no separate BRE engine, so its dialect may differ slightly from Git's). `-P`/`--perl-regexp` is declined (intentionally-different, exit 129): the linear-time engine has no backreferences/lookaround, so it fails fast rather than silently mis-matching. |
| blame | supported | `-L` ranges, `--json`/`--machine`, and display flags `-l`/`-t`/`-f`/`-n`/`-s`/`-e`/`-w` supported; `--porcelain`/`-p` supported; `-M`/`-C` partial (flags parsed but cross-file move/copy detection is not implemented — blame still walks this file) |
| revert | partial | Multi-commit and `A..B` range revert supported newest-first; detached-HEAD revert advances detached `HEAD`; `-n`/`--no-commit` supported for a single commit; `-m`/`--mainline <n>` merge-commit revert supported (required for merge commits, rejected for non-merge, exit 128); `-s`/`--signoff`; conflict sequencer controls `--continue`/`--abort`/`--skip`/`--quit` persist in SQLite `revert_sequence` rather than `.git`/`.libra` sequencer files. `-e`/`--edit` and `--no-edit` are accepted, but editor launch is deferred. Conflict detection is path-level, not Git's line-level hunk merge. Deferred: `--strategy`/`-X`, `-S`/`--gpg-sign`, `--cleanup`, `--commit`, `--rerere-autoupdate`, `--reference`, and Git's full `--no-*` alias set |
| remote | partial | `add`/`remove`/`rename`/`-v`/`show` (name list and `show <name>` detail with cached/offline fallback), `get-url`/`set-url`/`prune`, `set-branches [--add]` (rewrites `remote.<name>.fetch`), `set-head <branch>`/`--auto`/`-d` (writes/deletes `refs/remotes/<name>/HEAD`), and sequential `remote update [<remote>...]` supported. Intentional differences: `show <name>` discovery failures warn and fall back to cached refs with exit 0; `update` renders one aggregate JSON envelope and exits 128 on partial fetch failure. Remaining partial gaps: Git's cold flags (`add -t/-m/--mirror/-f/--tags`, full `set-url --push --add` combinations), `update -p`, and `remotes.<group>` / `remote.<name>.skipDefaultUpdate` groups |
| hash-object | partial | Hashing for `-t blob`/`commit`/`tree`/`tag` (files, `--stdin`, `--stdin-paths`), `-w` writes any of those object types, and `--literally` skips format validation. Without `--literally`, malformed commit/tree/tag input is rejected (exit 128) via command-layer header/binary checks (no parser panics). An unsupported `-t <type>` is a usage error (exit 129; intentionally-different from Git's 128). `--path` (a source label for `--stdin`, conflicts with `--no-filters`/`--stdin-paths`) and `--no-filters` (a no-op) are accepted; actual clean/smudge and CRLF filters are not implemented (Libra has no gitattributes/filter infrastructure). |
| open | supported | Libra extension (no `git open`). Resolves a remote/URL to a browsable web URL and launches the OS browser. Deep-link target flags `-b`/`--branch`, `-c`/`--commit`, `--issue[=<id>]`, `--pr[=<id>]` (mutually exclusive); multi-platform path assembly (github/gitlab/gitea/bitbucket) auto-detected from host and overridable via `open.platform` / `open.template.<kind>` local config; ref components are whitelist-validated (malicious input rejected with `LBR-CLI-003`); a missing launcher binary exits `0` with a manual-copy hint. JSON gains additive `target_type`/`platform` fields |
| config | supported | vault-backed; partial Git config parity with documented intentional differences (see [docs/commands/config.md](docs/commands/config.md) Git Config Compatibility Matrix) |
| db | intentionally-different | Libra repository database schema inspection/upgrade extension, not a Git command |
| reflog | supported | `show`/`delete`/`exists`/`expire` subcommands. `expire` prunes by time + reachability + `--stale-fix` (`--all`/`--expire`/`--expire-unreachable`/`--rewrite`/`--updateref`/`-n`/`-v`), reads `gc.reflogExpire`/`gc.reflogExpireUnreachable` (90/30-day defaults, never written). Intentional differences: no-ref expire is an explicit error (exit 128) vs Git's silent no-op; `--stale-fix` checks only that the new value loads as a commit (no transitive object walk); `--updateref` skips symbolic `HEAD` / remote-tracking refs |
| worktree | intentionally-different | `remove` keeps disk dir by default (no implicit data loss). Use `--delete-dir` for Git-style behavior; without `--force` it refuses dirty worktrees, one `--force` skips the dirty check, and `-f -f` is required to unregister locked worktrees. Partial Git-compatible surface: `add -b/-B`, `[commit-ish]`, `--detach`, `--no-checkout`, `--lock --reason`; `list --porcelain`/`--verbose`; `prune --dry-run`/`--verbose`/`--expire`; and `repair` rebuilding missing/stale linked `.libra` symlinks. Intentional/partial caveat: all Libra worktrees share one `.libra` storage and HEAD, so branch/detached state is not per-worktree and porcelain omits `branch`/`detached`. Unsupported/out of scope: `add -f/--force`, `--orphan`, explicit `--checkout`, and `list --porcelain -z`. |
| cloud | intentionally-different | Libra cloud backup/restore extension, not a Git command |
| publish | intentionally-different | Libra Cloudflare publish extension, not a Git command |
| agent | intentionally-different | Libra external-agent capture extension, not a Git command |
| hooks | intentionally-different | Hidden compatibility entry for hook configs installed by `libra agent enable` |
| cat-file | partial | Single-object `-t`/`-s`/`-p`/`-e` and `--ai*` supported. Batch protocol supported: `--batch`/`--batch-check` (with `=<format>` and `%(objectname)`/`%(objecttype)`/`%(objectsize)` atoms), `--batch-all-objects` (local loose+pack only — not un-fetched cloud-tiered objects), `--unordered`, `-Z` (NUL records), `--buffer`, `--follow-symlinks`. Batch per-line states: only `missing`/`ambiguous` (filtered/submodule deferred). `--follow-symlinks` resolves in the object graph only (never the working tree); escaping the tree root yields `missing` rather than Git's `symlink <size>` line (intentionally-different); link-follow depth capped at 32. 4 KiB cap on a batch input record (over-long → `LBR-CLI-003`/129; Libra hardening). `--textconv`/`--filters` rejected (`LBR-UNSUPPORTED-001`/128, intentionally-different). `--batch-command`/`--filter`/`--use-mailmap`/`--path`/bare `<type> <object>` form deferred. Deprecated lowercase `-z` deferred (only `-Z`). `-e` does not support JSON |
| fsck | supported | `--full` (default) verifies packed objects too: every `objects/pack/*.idx` (index v1 + v2) is parsed with `verify-pack`'s validated reader and merged with loose objects; `--no-full` restricts the check to loose objects. A dedicated pack-integrity stage runs regardless of `--full`: each `objects/pack/*.idx` is validated in-process via `verify-pack` (no subprocess); a corrupt or unreadable pack is reported and makes fsck exit 1 (matching `git fsck`) without aborting the remaining packs (fsck reports only — it never repairs a faulty pack). `--strict` adds commit email/timezone, commit tree/parent existence+type, and tree entry existence/type/sort-order checks (intentionally narrower than Git: `.gitmodules`/pathname-charset checks and `fsck.<msg-id>` severity config are not implemented). |
| gc | partial | Expires reflogs using the default `reflog expire` policy, then prunes unreachable loose objects and stale pack sidecars; full Git-style repack/delta compression/cruft packs are not implemented |
| prune | supported | |
| verify-pack | partial | validates `.idx` files against matching `.pack` files; multi-index and Git's `-s` / `--stat-only` are supported; `--pack` is a Libra-only explicit pack path. `-v` deltified-object lines omit Git's trailing `<chain-depth> <base-oid>` columns (intentionally-different: the `git-internal` decoder exposes the reconstructed object stream and `chain_len` but not the original delta base reference — chain depth is still observable via `--stat-only`). The core validation is reused in-process by `libra fsck` to health-check every packfile. |
| archive | partial | Creates archives from committed tree snapshots. Supports commit/branch/tag/short-hash tree-ish inputs, `tar`, `tar.gz`/`tgz`, `tar.bz2`/`tbz2`/`tbz`, `zip`, `--output`, and safe relative `--prefix`; pathspec filtering, attribute-based export-ignore/substitution, and remote archive protocol compatibility are not implemented. |
| index-pack | supported | hidden plumbing command |
| checkout | partial | branch compatibility surface plus path restoration. Branch modes `-b`, `-B` (create/reset), `--detach`, `--orphan`, and `-f`/`--force` supported; `--ours`/`--theirs` conflict-path checkout partial (restores one merge stage and collapses to stage 0; no `-p`/`--patch`, no `--conflict`); explicit `checkout [<tree-ish>] -- <path>` restoration alias supported. Plain `checkout <commit-ish>`/`<tag>` without `--detach`, `-p`/`--patch` (see [docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族](docs/improvement/compatibility/declined.md#d15-交互式补丁选择-ui-拒绝p--patch-跨命令族)), and `--conflict` deferred. Internal `intent`/`agent-traces` branches protected. Prefer `switch` / `restore` |
| bisect | partial | `start` / `bad` / `good` / `reset` / `skip` / `log` / `run` / `view` supported; `start` accepts `<bad> <good>...` positional bounds (multiple good commits); `replay` (see [docs/improvement/compatibility/declined.md#d6-bisect-replay](docs/improvement/compatibility/declined.md#d6-bisect-replay)) / `terms` (see [docs/improvement/compatibility/declined.md#d7-bisect-terms](docs/improvement/compatibility/declined.md#d7-bisect-terms)) deferred; `start -- <pathspec>` rejected (path-limited bisect unsupported) |

## Git commands intentionally absent from `src/cli.rs`

| Command | Tier | Notes |
|---------|------|-------|
| submodule | unsupported | intentional product boundary (see [docs/improvement/compatibility/declined.md#d1-submodule-子命令族](docs/improvement/compatibility/declined.md#d1-submodule-子命令族)) |
| sparse-checkout | unsupported | no public sparse checkout command (see [docs/improvement/compatibility/declined.md#d10-clone---sparse-与顶层-sparse-checkout-命令](docs/improvement/compatibility/declined.md#d10-clone---sparse-与顶层-sparse-checkout-命令)) |

## Hooks

- Stock Git hooks at `.git/hooks` / `core.hooksPath`: `unsupported` (see [docs/improvement/compatibility/declined.md#d3-git-hooks-bridge-作为核心特性](docs/improvement/compatibility/declined.md#d3-git-hooks-bridge-作为核心特性))
- Libra-native commit hooks `pre-commit` / `prepare-commit-msg` / `commit-msg` run from `.libra/hooks/*.sh` (Unix) / `*.ps1` (Windows): `intentionally-different`. These are Libra's own hook surface and do **not** read or interoperate with stock `.git/hooks` or `core.hooksPath`. `post-commit` is not implemented (deferred)
- AI provider hooks: `intentionally-different` (see [docs/improvement/agent.md](docs/improvement/agent.md))

## LFS compatibility notes

- `libra lfs`: `partial` command compatibility. Libra uses built-in pointer /
  lock management and `.libra_attributes`.
- Git LFS filter bridge (`.gitattributes` smudge/clean filters + `git-lfs` hook
  install): `intentionally-different` (see
  [docs/improvement/compatibility/declined.md#d5-git-lfs-gitattributes-filter--hooks-bridge](docs/improvement/compatibility/declined.md#d5-git-lfs-gitattributes-filter--hooks-bridge)).
- Repository asset storage policy: current committed binaries remain inline.
  Optional future Git LFS rules in `.gitattributes` are tracked as a repository
  governance decision, **not** as the `libra lfs` command status.
