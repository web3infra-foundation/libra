// Vitest stub: the production "server-only" package throws when
// imported into a Client Component bundle. Tests run in Node only,
// so we expose a no-op module so server modules can be exercised.
export {};
