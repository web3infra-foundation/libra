# @generated by `cargo buckal`

load("@buckal//:cargo_manifest.bzl", "cargo_manifest")
load("@buckal//:wrapper.bzl", "buildscript_run", "rust_binary", "rust_library")

http_archive(
    name = "zeroize-vendor",
    urls = ["https://static.crates.io/crates/zeroize/zeroize-1.8.1.crate"],
    sha256 = "ced3678a2879b30306d323f4542626697a464a97c0a07c9aebf7ebca65cd4dde",
    type = "tar.gz",
    strip_prefix = "zeroize-1.8.1",
    out = "vendor",
)

cargo_manifest(
    name = "zeroize-manifest",
    vendor = ":zeroize-vendor",
)

rust_library(
    name = "zeroize",
    srcs = [":zeroize-vendor"],
    crate = "zeroize",
    crate_root = "vendor/src/lib.rs",
    edition = "2021",
    features = [
        "alloc",
        "default",
        "zeroize_derive",
    ],
    rustc_flags = ["@$(location :zeroize-manifest[env_flags])"],
    visibility = ["PUBLIC"],
    deps = ["//third-party/rust/crates/zeroize_derive/1.4.2:zeroize_derive"],
)
