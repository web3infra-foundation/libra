# @generated by `cargo buckal`

load("@buckal//:cargo_manifest.bzl", "cargo_manifest")
load("@buckal//:wrapper.bzl", "buildscript_run", "rust_binary", "rust_library")

http_archive(
    name = "pack1-vendor",
    urls = ["https://static.crates.io/crates/pack1/pack1-1.0.0.crate"],
    sha256 = "d6e7cd9bd638dc2c831519a0caa1c006cab771a92b1303403a8322773c5b72d6",
    type = "tar.gz",
    strip_prefix = "pack1-1.0.0",
    out = "vendor",
)

cargo_manifest(
    name = "pack1-manifest",
    vendor = ":pack1-vendor",
)

rust_library(
    name = "pack1",
    srcs = [":pack1-vendor"],
    crate = "pack1",
    crate_root = "vendor/src/lib.rs",
    edition = "2021",
    features = ["bytemuck"],
    rustc_flags = ["@$(location :pack1-manifest[env_flags])"],
    visibility = ["PUBLIC"],
    deps = ["//third-party/rust/crates/bytemuck/1.24.0:bytemuck"],
)
