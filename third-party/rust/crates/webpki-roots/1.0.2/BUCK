# @generated by `cargo buckal`

load("@buckal//:cargo_manifest.bzl", "cargo_manifest")
load("@buckal//:wrapper.bzl", "buildscript_run", "rust_binary", "rust_library")

http_archive(
    name = "webpki-roots-vendor",
    urls = ["https://static.crates.io/crates/webpki-roots/webpki-roots-1.0.2.crate"],
    sha256 = "7e8983c3ab33d6fb807cfcdad2491c4ea8cbc8ed839181c7dfd9c67c83e261b2",
    type = "tar.gz",
    strip_prefix = "webpki-roots-1.0.2",
    out = "vendor",
)

cargo_manifest(
    name = "webpki-roots-manifest",
    vendor = ":webpki-roots-vendor",
)

rust_library(
    name = "webpki-roots",
    srcs = [":webpki-roots-vendor"],
    crate = "webpki_roots",
    crate_root = "vendor/src/lib.rs",
    edition = "2021",
    rustc_flags = ["@$(location :webpki-roots-manifest[env_flags])"],
    named_deps = {
        "pki_types": "//third-party/rust/crates/rustls-pki-types/1.12.0:rustls-pki-types",
    },
    visibility = ["PUBLIC"],
)
