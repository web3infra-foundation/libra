{
  "responses": [
    {
      "match": { "contains": "slow" },
      "type": "text",
      "delayMs": 5000,
      "text": "explorer sub-agent: slow response that the parent should never see if cancel races correctly"
    }
  ],
  "fallback": {
    "type": "text",
    "text": "explorer sub-agent: fallback (no slow match)"
  }
}
